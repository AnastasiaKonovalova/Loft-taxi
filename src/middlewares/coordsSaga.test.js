import { all, put, call, take, takeEvery } from "redux-saga/effects";

import {getCoordsWatcher} from './coordsSaga';
import {fetchCoordsRequest, fetchCoordsSuccess, fetchCoordsFailure} from '../actions';

describe('getCoordsWatcher', () => {
    
})